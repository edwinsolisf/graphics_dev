#include "Texture.h"

Texture::Texture(const std::string& fileName)
{
    //GLCALL(glGenTextures(1, &id_));
    //GLCALL(glBindTexture(id_));
    //GLCALL(glTexImage2D());
}

Texture::~Texture()
{

}
